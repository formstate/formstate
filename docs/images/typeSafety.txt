function demo() {
  const username = new FieldState({ value: '' });
  const foo: number = username.safeValue; // TS Error: cannot assign string to number

  const form = new FormState({
    username
  });
  const bar: number = form.safeValue.username; // TS Error: cannot assign string to number
}


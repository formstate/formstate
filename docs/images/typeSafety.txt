async function demo() {
  const username = new FieldState({ value: '' });
  const foo: number = username.value; // TS Error: cannot assign string to number

  const form = new FormState({
    username
  });
  const bar: number = form.value.username.value; // TS Error: cannot assign string to number
}

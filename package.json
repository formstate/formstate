{
  "name": "formstate",
  "version": "0.0.0",
  "description": "Painless and simple MobX form management",
  "main": "./lib/index.js",
  "typings": "./lib/index",
  "scripts": {
    "build": "tsc -p .",
    "unittest": "mocha ./lib/tests/**/*.js",
    "test": "npm run build && npm run unittest",
    "preversion": "npm run build",
    "postversion": "git push --follow-tags && npm publish",
    "start": "npm run build -- -w & npm run unittest -- --watch & docute ./docs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/formstate/formstate.git"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/formstate/formstate/issues"
  },
  "homepage": "https://github.com/formstate/formstate#readme",
  "peerDependencies": {
    "mobx": ">=2.0.0"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/mocha": "^2.2.32",
    "@types/node": "^6.0.45",
    "mobx": ">=2.0.0",
    "mocha": "^3.1.2",
    "ts-node": "^1.5.2",
    "typescript": "2.2.0-dev.20161130"
  }
}
